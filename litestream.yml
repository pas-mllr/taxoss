dbs:
  - path: ${DATABASE_PATH}
    replicas:
      - type: abs
        account-name: ${AZURE_STORAGE_ACCOUNT}
        account-key: ${AZURE_STORAGE_KEY}
        bucket: ${LITESTREAM_BUCKET}
        path: taxoss/app.db
